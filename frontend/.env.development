# Development Environment Variables
# Backend API will be proxied through Vite dev server
# No need to set VITE_API_URL here, it will use the default '/api' from api.js

# Optional: Set if you want to override the API URL
# VITE_API_URL=/api
